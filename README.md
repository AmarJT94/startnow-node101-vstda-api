Run npm install, npm install express, npm install morgan, and npm install body-parser.
Gives an example of how to do get and post using postman as well as  how the server handles them depending on setup.
